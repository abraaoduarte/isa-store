import { styled } from '@mui/material/styles';
import { Box } from '@mui/system';

export const Footer = styled(Box)({
  position: 'fixed',
  bottom: 0,
  padding: 16,
  width: '100%',
  left: 0,
});
