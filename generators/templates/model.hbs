import Model from './Model';

class {{pascalCase name}} extends Model {
  public id: string;

  static tableName = '{{lowerCase name}}s';
  static useSoftDelete = true;

  static jsonSchema = {
    type: 'object',
    required: [],

    properties: {
      id: { type: 'string' },
    }
  };
}

export default {{pascalCase name}};
